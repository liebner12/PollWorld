import { colors, backgroundColors } from "./colors";
export const darkTheme = {
  dark: true,
  colors: {
    primary: colors.primary,
    background: backgroundColors.primary,
    card: backgroundColors.secondary,
    text: colors.white,
    border: colors.dark,
    notification: colors.error,
  },
};
